
public class GUInterface {
	//component is anything that can appear / display in window
	//container is something that contains containers/component
	
	//a container is a component, 
	//so it can be contained being a component or contains other component
	//when a resize happens to a container, it can trigger
	//message to all of its components that do something....resize
	
	//we can have listeners for components that are resize
	
	
}
